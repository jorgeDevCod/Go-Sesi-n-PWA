{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy || true && prisma generate && next build",
  "installCommand": "npm install"
}
